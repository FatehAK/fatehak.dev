---
import 'theme/theme.css';
import 'theme/base.css';
import FontFace from 'components/FontFace.astro';
import Navbar from 'components/Navbar.astro';
import Footer from 'components/Footer.astro';
import { APP_CONFIG } from 'appConfig';

const isProd = import.meta.env.BUILD === 'production';
---

<!DOCTYPE html>
<html class="theme-default" lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="X-UA-Compatible" content="IE=edge, chrome=1" />
    <slot name="seo" />
    <script is:inline>
      // figure out user's preferred theme and set it as html class
      (function () {
        if (typeof window !== 'undefined') {
          const storageTheme = sessionStorage.getItem('theme');
          if (!storageTheme || storageTheme === 'dark') {
            document.documentElement.classList.add('dark');
          }
        }
      })();
    </script>
    <FontFace />
    <link href="/fontawesome/css/bareminimum.css" rel="stylesheet" />
    {isProd && <script src={`https://static.cloudflareinsights.com/beacon.min.js?token=${APP_CONFIG.CLOUDFLARE_ANALYTICS_TOKEN}`} defer />}
  </head>
  <body class="h-full max-w-4xl mx-auto px-8 sm:px-6">
    <Navbar />
    <main id="main">
      <slot />
    </main>
    <Footer />
    <style>
      body {
        display: grid;
        grid-template-areas:
          'header'
          'main'
          'footer';
        grid-template-columns: minmax(0, 1fr);
        grid-template-rows: 5rem minmax(0, 1fr) 5rem;
      }

      main {
        grid-area: main;
      }
    </style>
  </body>
</html>
